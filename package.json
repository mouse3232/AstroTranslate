{
  "name": "ai-translation-and-correction",
  "version": "0.0.0",
  "private": true,
  "type": "module",   // can keep this
  "bin": "launcher.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:frontend": "vite build",
    "build:exe": "pkg ."
  },
  "dependencies": { ... },
  "devDependencies": { ... },
  "pkg": {
    "assets": [
      "dist/**/*",
      "server.js",
      "cli.js",
      "services/**/*",
      "utils/**/*",
      "metadata.json"
    ],
    "targets": ["node18-win-x64"],
    "outputPath": "release"
  }
}
